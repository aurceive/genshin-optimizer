# gi-lapic-adapter

Genshin Impact adapter surface for lapic normalization and bounded runtime bridging.

This package owns GI request normalization, candidate-domain projection, metadata and capability reporting, canonical problem export, and the bounded current-only bridge into lapic runtime.
